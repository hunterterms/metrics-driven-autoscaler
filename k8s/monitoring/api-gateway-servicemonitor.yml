apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-gateway
  labels:
    release: monitoring
spec:
  endpoints:
    - port: http
      path: /metrics
      interval: 5s
  selector:
    matchLabels:
      app: api-gateway
